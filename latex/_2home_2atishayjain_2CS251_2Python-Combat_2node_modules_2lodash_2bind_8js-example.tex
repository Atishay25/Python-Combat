\hypertarget{_2home_2atishayjain_2CS251_2Python-Combat_2node_modules_2lodash_2bind_8js-example}{}\doxysection{/home/atishayjain/\+C\+S251/\+Python-\/\+Combat/node\+\_\+modules/lodash/bind.\+js}
function greet(greeting, punctuation) \{ return greeting + \textquotesingle{} \textquotesingle{} + this.\+user + punctuation; \}

var object = \{ \textquotesingle{}user\textquotesingle{}\+: \textquotesingle{}fred\textquotesingle{} \};

var bound = \+\_\+.\+bind(greet, object, \textquotesingle{}hi\textquotesingle{}); bound(\textquotesingle{}!\textquotesingle{}); // =$>$ \textquotesingle{}hi fred!\textquotesingle{}

// Bound with placeholders. var bound = \+\_\+.\+bind(greet, object, \+\_\+, \textquotesingle{}!\textquotesingle{}); bound(\textquotesingle{}hi\textquotesingle{}); // =$>$ \textquotesingle{}hi fred!\textquotesingle{}


\begin{DoxyCodeInclude}{0}
\DoxyCodeLine{var baseRest = require(\textcolor{stringliteral}{'./\_baseRest'}),}
\DoxyCodeLine{    createWrap = require(\textcolor{stringliteral}{'./\_createWrap'}),}
\DoxyCodeLine{    getHolder = require(\textcolor{stringliteral}{'./\_getHolder'}),}
\DoxyCodeLine{    replaceHolders = require(\textcolor{stringliteral}{'./\_replaceHolders'});}
\DoxyCodeLine{}
\DoxyCodeLine{var WRAP\_BIND\_FLAG = 1,}
\DoxyCodeLine{    WRAP\_PARTIAL\_FLAG = 32;}
\DoxyCodeLine{}
\DoxyCodeLine{var bind = baseRest(\textcolor{keyword}{function}(func, thisArg, partials) \{}
\DoxyCodeLine{  var bitmask = WRAP\_BIND\_FLAG;}
\DoxyCodeLine{  \textcolor{keywordflow}{if} (partials.length) \{}
\DoxyCodeLine{    var holders = replaceHolders(partials, getHolder(bind));}
\DoxyCodeLine{    bitmask |= WRAP\_PARTIAL\_FLAG;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{return} createWrap(func, bitmask, thisArg, partials, holders);}
\DoxyCodeLine{\});}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// Assign default placeholders.}}
\DoxyCodeLine{bind.placeholder = \{\};}
\DoxyCodeLine{}
\DoxyCodeLine{module.exports = bind;}
\end{DoxyCodeInclude}
 