\hypertarget{_2home_2atishayjain_2CS251_2Python-Combat_2node_modules_2lodash_2overArgs_8js-example}{}\doxysection{/home/atishayjain/\+C\+S251/\+Python-\/\+Combat/node\+\_\+modules/lodash/over\+Args.\+js}
function doubled(n) \{ return n $\ast$ 2; \}

function square(n) \{ return n $\ast$ n; \}

var func = \+\_\+.\+over\+Args(function(x, y) \{ return \mbox{[}x, y\mbox{]}; \}, \mbox{[}square, doubled\mbox{]});

func(9, 3); // =$>$ \mbox{[}81, 6\mbox{]}

func(10, 5); // =$>$ \mbox{[}100, 10\mbox{]}


\begin{DoxyCodeInclude}{0}
\DoxyCodeLine{var apply = require(\textcolor{stringliteral}{'./\_apply'}),}
\DoxyCodeLine{    arrayMap = require(\textcolor{stringliteral}{'./\_arrayMap'}),}
\DoxyCodeLine{    baseFlatten = require(\textcolor{stringliteral}{'./\_baseFlatten'}),}
\DoxyCodeLine{    baseIteratee = require(\textcolor{stringliteral}{'./\_baseIteratee'}),}
\DoxyCodeLine{    baseRest = require(\textcolor{stringliteral}{'./\_baseRest'}),}
\DoxyCodeLine{    baseUnary = require(\textcolor{stringliteral}{'./\_baseUnary'}),}
\DoxyCodeLine{    castRest = require(\textcolor{stringliteral}{'./\_castRest'}),}
\DoxyCodeLine{    isArray = require(\textcolor{stringliteral}{'./isArray'});}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{/* Built-\/in method references for those with the same name as other `lodash` methods. */}}
\DoxyCodeLine{var nativeMin = Math.min;}
\DoxyCodeLine{}
\DoxyCodeLine{var overArgs = castRest(\textcolor{keyword}{function}(func, transforms) \{}
\DoxyCodeLine{  transforms = (transforms.length == 1 \&\& isArray(transforms[0]))}
\DoxyCodeLine{    ? arrayMap(transforms[0], baseUnary(baseIteratee))}
\DoxyCodeLine{    : arrayMap(baseFlatten(transforms, 1), baseUnary(baseIteratee));}
\DoxyCodeLine{}
\DoxyCodeLine{  var funcsLength = transforms.length;}
\DoxyCodeLine{  \textcolor{keywordflow}{return} baseRest(\textcolor{keyword}{function}(args) \{}
\DoxyCodeLine{    var index = -\/1,}
\DoxyCodeLine{        length = nativeMin(args.length, funcsLength);}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{keywordflow}{while} (++index < length) \{}
\DoxyCodeLine{      args[index] = transforms[index].call(\textcolor{keyword}{this}, args[index]);}
\DoxyCodeLine{    \}}
\DoxyCodeLine{    \textcolor{keywordflow}{return} apply(func, \textcolor{keyword}{this}, args);}
\DoxyCodeLine{  \});}
\DoxyCodeLine{\});}
\DoxyCodeLine{}
\DoxyCodeLine{module.exports = overArgs;}
\end{DoxyCodeInclude}
 