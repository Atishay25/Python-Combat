\hypertarget{_2home_2atishayjain_2CS251_2Python-Combat_2node_modules_2lodash_2throttle_8js-example}{}\doxysection{/home/atishayjain/\+C\+S251/\+Python-\/\+Combat/node\+\_\+modules/lodash/throttle.\+js}
// Avoid excessively updating the position while scrolling. j\+Query(window).on(\textquotesingle{}scroll\textquotesingle{}, \+\_\+.\+throttle(update\+Position, 100));

// Invoke {\ttfamily renew\+Token} when the click event is fired, but not more than once every 5 minutes. var throttled = \+\_\+.\+throttle(renew\+Token, 300000, \{ \textquotesingle{}trailing\textquotesingle{}\+: false \}); j\+Query(element).on(\textquotesingle{}click\textquotesingle{}, throttled);

// Cancel the trailing throttled invocation. j\+Query(window).on(\textquotesingle{}popstate\textquotesingle{}, throttled.\+cancel);


\begin{DoxyCodeInclude}{0}
\DoxyCodeLine{var debounce = require(\textcolor{stringliteral}{'./debounce'}),}
\DoxyCodeLine{    isObject = require(\textcolor{stringliteral}{'./isObject'});}
\DoxyCodeLine{}
\DoxyCodeLine{var FUNC\_ERROR\_TEXT = \textcolor{stringliteral}{'Expected a function'};}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{function} throttle(func, wait, options) \{}
\DoxyCodeLine{  var leading = \textcolor{keyword}{true},}
\DoxyCodeLine{      trailing = \textcolor{keyword}{true};}
\DoxyCodeLine{}
\DoxyCodeLine{  \textcolor{keywordflow}{if} (typeof func != \textcolor{stringliteral}{'function'}) \{}
\DoxyCodeLine{    \textcolor{keywordflow}{throw} \textcolor{keyword}{new} TypeError(FUNC\_ERROR\_TEXT);}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{if} (isObject(options)) \{}
\DoxyCodeLine{    leading = \textcolor{stringliteral}{'leading'} in options ? !!options.leading : leading;}
\DoxyCodeLine{    trailing = \textcolor{stringliteral}{'trailing'} in options ? !!options.trailing : trailing;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{return} debounce(func, wait, \{}
\DoxyCodeLine{    \textcolor{stringliteral}{'leading'}: leading,}
\DoxyCodeLine{    \textcolor{stringliteral}{'maxWait'}: wait,}
\DoxyCodeLine{    \textcolor{stringliteral}{'trailing'}: trailing}
\DoxyCodeLine{  \});}
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{module.exports = throttle;}
\end{DoxyCodeInclude}
 