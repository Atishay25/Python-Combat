\hypertarget{_2home_2atishayjain_2CS251_2Python-Combat_2node_modules_2lodash_2random_8js-example}{}\doxysection{/home/atishayjain/\+C\+S251/\+Python-\/\+Combat/node\+\_\+modules/lodash/random.\+js}
\+\_\+.\+random(0, 5); // =$>$ an integer between 0 and 5

\+\_\+.\+random(5); // =$>$ also an integer between 0 and 5

\+\_\+.\+random(5, true); // =$>$ a floating-\/point number between 0 and 5

\+\_\+.\+random(1.\+2, 5.\+2); // =$>$ a floating-\/point number between 1.\+2 and 5.\+2


\begin{DoxyCodeInclude}{0}
\DoxyCodeLine{var baseRandom = require(\textcolor{stringliteral}{'./\_baseRandom'}),}
\DoxyCodeLine{    isIterateeCall = require(\textcolor{stringliteral}{'./\_isIterateeCall'}),}
\DoxyCodeLine{    toFinite = require(\textcolor{stringliteral}{'./toFinite'});}
\DoxyCodeLine{}
\DoxyCodeLine{var freeParseFloat = parseFloat;}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{/* Built-\/in method references for those with the same name as other `lodash` methods. */}}
\DoxyCodeLine{var nativeMin = Math.min,}
\DoxyCodeLine{    nativeRandom = Math.random;}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{function} random(lower, upper, floating) \{}
\DoxyCodeLine{  \textcolor{keywordflow}{if} (floating \&\& typeof floating != \textcolor{stringliteral}{'boolean'} \&\& isIterateeCall(lower, upper, floating)) \{}
\DoxyCodeLine{    upper = floating = undefined;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{if} (floating === undefined) \{}
\DoxyCodeLine{    \textcolor{keywordflow}{if} (typeof upper == \textcolor{stringliteral}{'boolean'}) \{}
\DoxyCodeLine{      floating = upper;}
\DoxyCodeLine{      upper = undefined;}
\DoxyCodeLine{    \}}
\DoxyCodeLine{    \textcolor{keywordflow}{else} \textcolor{keywordflow}{if} (typeof lower == \textcolor{stringliteral}{'boolean'}) \{}
\DoxyCodeLine{      floating = lower;}
\DoxyCodeLine{      lower = undefined;}
\DoxyCodeLine{    \}}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{if} (lower === undefined \&\& upper === undefined) \{}
\DoxyCodeLine{    lower = 0;}
\DoxyCodeLine{    upper = 1;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{else} \{}
\DoxyCodeLine{    lower = toFinite(lower);}
\DoxyCodeLine{    \textcolor{keywordflow}{if} (upper === undefined) \{}
\DoxyCodeLine{      upper = lower;}
\DoxyCodeLine{      lower = 0;}
\DoxyCodeLine{    \} \textcolor{keywordflow}{else} \{}
\DoxyCodeLine{      upper = toFinite(upper);}
\DoxyCodeLine{    \}}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{if} (lower > upper) \{}
\DoxyCodeLine{    var temp = lower;}
\DoxyCodeLine{    lower = upper;}
\DoxyCodeLine{    upper = temp;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{if} (floating || lower \% 1 || upper \% 1) \{}
\DoxyCodeLine{    var rand = nativeRandom();}
\DoxyCodeLine{    \textcolor{keywordflow}{return} nativeMin(lower + (rand * (upper -\/ lower + freeParseFloat(\textcolor{stringliteral}{'1e-\/'} + ((rand + \textcolor{stringliteral}{''}).length -\/ 1)))), upper);}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{return} baseRandom(lower, upper);}
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{module.exports = random;}
\end{DoxyCodeInclude}
 