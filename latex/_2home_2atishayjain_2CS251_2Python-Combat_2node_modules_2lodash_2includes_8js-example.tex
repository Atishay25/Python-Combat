\hypertarget{_2home_2atishayjain_2CS251_2Python-Combat_2node_modules_2lodash_2includes_8js-example}{}\doxysection{/home/atishayjain/\+C\+S251/\+Python-\/\+Combat/node\+\_\+modules/lodash/includes.\+js}
\+\_\+.\+includes(\mbox{[}1, 2, 3\mbox{]}, 1); // =$>$ true

\+\_\+.\+includes(\mbox{[}1, 2, 3\mbox{]}, 1, 2); // =$>$ false

\+\_\+.\+includes(\{ \textquotesingle{}a\textquotesingle{}\+: 1, \textquotesingle{}b\textquotesingle{}\+: 2 \}, 1); // =$>$ true

\+\_\+.\+includes(\textquotesingle{}abcd\textquotesingle{}, \textquotesingle{}bc\textquotesingle{}); // =$>$ true


\begin{DoxyCodeInclude}{0}
\DoxyCodeLine{var baseIndexOf = require(\textcolor{stringliteral}{'./\_baseIndexOf'}),}
\DoxyCodeLine{    isArrayLike = require(\textcolor{stringliteral}{'./isArrayLike'}),}
\DoxyCodeLine{    isString = require(\textcolor{stringliteral}{'./isString'}),}
\DoxyCodeLine{    toInteger = require(\textcolor{stringliteral}{'./toInteger'}),}
\DoxyCodeLine{    values = require(\textcolor{stringliteral}{'./values'});}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{/* Built-\/in method references for those with the same name as other `lodash` methods. */}}
\DoxyCodeLine{var nativeMax = Math.max;}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{function} includes(collection, value, fromIndex, guard) \{}
\DoxyCodeLine{  collection = isArrayLike(collection) ? collection : values(collection);}
\DoxyCodeLine{  fromIndex = (fromIndex \&\& !guard) ? toInteger(fromIndex) : 0;}
\DoxyCodeLine{}
\DoxyCodeLine{  var length = collection.length;}
\DoxyCodeLine{  \textcolor{keywordflow}{if} (fromIndex < 0) \{}
\DoxyCodeLine{    fromIndex = nativeMax(length + fromIndex, 0);}
\DoxyCodeLine{  \}}
\DoxyCodeLine{  \textcolor{keywordflow}{return} isString(collection)}
\DoxyCodeLine{    ? (fromIndex <= length \&\& collection.indexOf(value, fromIndex) > -\/1)}
\DoxyCodeLine{    : (!!length \&\& baseIndexOf(collection, value, fromIndex) > -\/1);}
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{module.exports = includes;}
\end{DoxyCodeInclude}
 